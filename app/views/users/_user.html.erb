<div class="box user">

  <%= link_to image_tag(user.avatar.url(:thumb), :alt => user.name), user %>

  <h2><%= link_to user.name, user %></h2>

  <%= tagline(user) %>

  <p><%= link_to t("videos.count", :number => user.videos.count), user %></p>

</div>
